{
  "name": "rn-select-option",
  "version": "0.0.9",
  "description": "React Native component which is like `SELECT` tag in HTML. It wraps `Picker` component of '@react-native-picker/picker' package",
  "author": "AT Mulyana <atmulyana@yahoo.com>",
  "license": "MIT",
  "repository": "https://github.com/atmulyana/rn-select-option",
  "keywords": [
    "react-native",
    "react native",
    "select",
    "picker",
    "dropdown",
    "input"
  ],
  "main": "index.js",
  "scripts": {
    "flow": "flow"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "dependencies": {
    "reactjs-common": ">=0.2.0",
    "rn-style-props": "*"
  },
  "peerDependencies": {
    "@react-native-picker/picker": ">=2.0.0",
    "react": ">=16.8.0",
    "react-native": ">=0.57"
  },
  "devDependencies": {
    "flow-bin": "^0.275.0",
    "react": "18.2.0",
    "react-native": "0.71.0"
  }
}
